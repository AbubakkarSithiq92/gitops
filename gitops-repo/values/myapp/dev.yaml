metadata:
  name: myapp-dev

replicaCount: 1

image:
  repository: nginx
  tag: "alpine"

hpa:
  enabled: true
  min: 1
  max: 3

ingress:
  host: app.abulearning.in

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 256Mi

envName: DEV
message: "Hello from DEV 🚀 version01"
